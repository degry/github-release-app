export enum ReleasePullStatus {
  NOT_CREATED = 'NOT_CREATED',
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
  MERGED = 'MERGED',
  DECLINED = 'DECLINED'
}

export enum Labels {
  RELEASE = 'release'
}
